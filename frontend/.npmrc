# 핸드오프 안정성: bleeding-edge devDep 들의 optional peer(neo4j-driver, babel 컴파일러 등)
# 로 인한 npm ERESOLVE 를 막는다. 앱 실행/빌드에는 불필요한 peer 라 무시해도 안전.
legacy-peer-deps=true
# Vite/Node 엔진 경고로 설치가 막히지 않게(구버전 Node 에서도 설치 진행).
engine-strict=false
